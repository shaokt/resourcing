import { assignmentShowTiles } from '../../../helpers/assignment-show-tiles';
import { module, test } from 'qunit';

module('Unit | Helper | assignment show tiles');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assignmentShowTiles(42);
  assert.ok(result);
});
