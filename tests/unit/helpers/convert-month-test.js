import { convertMonth } from '../../../helpers/convert-month';
import { module, test } from 'qunit';

module('Unit | Helper | convert month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = convertMonth(42);
  assert.ok(result);
});
