import { gridCss } from '../../../helpers/grid-css';
import { module, test } from 'qunit';

module('Unit | Helper | grid css');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = gridCss(42);
  assert.ok(result);
});
