import { assignmentViewPeople } from '../../../helpers/assignment-view-people';
import { module, test } from 'qunit';

module('Unit | Helper | assignment view people');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = assignmentViewPeople(42);
  assert.ok(result);
});
