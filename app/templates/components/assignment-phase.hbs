{{#if shift}}
    {{input type="radio" id=id name="phase" action="shift" on="focus-in"}}
{{else}}
    {{input type="radio" id=id name="phase" action="setStamp" on="focus-in"}}
{{/if}}
<label for="phase{{label}}">{{label}}</label>
