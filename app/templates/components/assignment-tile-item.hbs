<div class="tileContainer" data-container="{{item.id}}">
    <a href="#" data-assignment="{{item.id}}" data-stamp="true" {{action "select" on="click" event=event}}>
        <span></span>
    </a>
    {{#unless paint}}
        <input type="checkbox" id={{concat 'ass-' item.id '-v'}} checked={{item.selected}} onclick={{action "select" event=event}}>
    {{/unless}}
</div>
