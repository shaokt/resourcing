<style>
    [data-type="todayDateLine"] { left:{{constants.todayColumn}}px; }
    .assignmentViewContainer { width:{{constants.calWidth}}px; }

    {{#if showAssignment}}
        .rowContainer { padding-top:{{numAssignmentsViewing}}px;}
    {{/if}}

    {{#if showInRoadmap}}
        [data-type="assignment"] { display:none; }
        {{roadmapList}} { display:block; }
    {{/if}}

    {{counterCSS}}
</style>

<style id="dynamicLeftScroll"></style>
<style id="dynamicMousePosition"></style>
